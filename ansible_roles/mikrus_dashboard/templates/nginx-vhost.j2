server {
	listen 80;
        listen [::]:80;
        root /var/www/{{ item.server_name }};
        index index.html;
        server name {{ item.server_name }} www.{{ item.server_name }};
        
	location / {
    		try_files $uri $uri/ =404;
  	}
}
